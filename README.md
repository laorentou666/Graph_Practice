# Graph Practice

数据结构实践实验二：景区路径规划系统中的图存储结构实现。

- A 角色：邻接表实现，包含创建图、添加景点、添加路径、显示景区图、显示景点链表。
- B 角色：邻接矩阵实现，合并后与邻接表代码在同一工程中演示。

## Build

Windows PowerShell:

```powershell
.\build.ps1
.\build\graph_practice.exe
```

Make:

```bash
make run
```
